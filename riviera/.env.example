# Server-side only. Deliberately NOT prefixed with VITE_, so Vite will never
# inline it into the browser bundle. Copy this file to .env.local for local
# development, and set the same variable in your hosting provider for deploys.
ANTHROPIC_API_KEY=sk-ant-...
